import { HighlightPipe } from './highlight.pipe';

/**
 * Tests for `HighlightPipe`.
 */
describe('HighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
